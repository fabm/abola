verticles:
  dao: pt.fabm.abola.EBForwarderVerticleTest
  rest: pt.fabm.abola.rest.RestVerticle
confs:
  rest:
    port: 8888
    host: localhost
  dao:
    #all the event bus consumers addresses to be forwarded
    eb_consumers:
      -  dao.user.create
      -  dao.user.login
      -  dao.reservation.list
      -  dao.reservation.create
    codecs:
      dao.reservation.list: List
